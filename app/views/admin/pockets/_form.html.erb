<%= form_for([:admin, @pocket]) do |f| %>
  <% if @pocket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pocket.errors.count, "error") %> prohibited this pocket from being saved:</h2>

      <ul>
      <% @pocket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
